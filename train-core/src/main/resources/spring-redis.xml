<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">

    <!-- jedisPool-->
    <bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
        <property name="maxIdle" value="${redis.maxIdle}" />
        <property name="maxTotal" value="${redis.maxTotal}" />
        <property name="maxWaitMillis" value="${redis.maxWaitMillis}" />
        <property name="testOnBorrow" value="${redis.testOnBorrow}" />
    </bean>

    <!--  外网-订单推送 -->
    <bean id="orderJedisClient" class="com.kuyou.train.common.jedis.JedisClient">
         <property name="jedisPool">
             <bean class="redis.clients.jedis.JedisPool">
                 <constructor-arg index="0" ref="jedisPoolConfig" />
                 <constructor-arg index="1" value="${public.host}" />
                 <constructor-arg index="2" value="${public.port}" type="int" />
                 <constructor-arg index="3" value="${public.timeout}" type="int" />
                 <constructor-arg index="4" value="${public.password}" />
                 <constructor-arg index="5" value="${public.order.db}" type="int" />
             </bean>
         </property>
    </bean>

    <!-- 内网-锁 -->
    <bean id="lockJedisClient" class="com.kuyou.train.common.jedis.JedisClient">
         <property name="jedisPool">
             <bean class="redis.clients.jedis.JedisPool">
                 <constructor-arg index="0" ref="jedisPoolConfig" />
                 <constructor-arg index="1" value="${private.host}" />
                 <constructor-arg index="2" value="${private.port}" type="int" />
                 <constructor-arg index="3" value="${private.timeout}" type="int" />
                 <constructor-arg index="4" value="${private.password}" />
                 <constructor-arg index="5" value="${private.lock.db}" type="int" />
             </bean>
         </property>
    </bean>

    <!-- 内网-基础数据 -->
    <bean id="dataJedisClient" class="com.kuyou.train.common.jedis.JedisClient">
        <property name="jedisPool">
            <bean class="redis.clients.jedis.JedisPool">
                <constructor-arg index="0" ref="jedisPoolConfig" />
                <constructor-arg index="1" value="${private.host}" />
                <constructor-arg index="2" value="${private.port}" type="int" />
                <constructor-arg index="3" value="${private.timeout}" type="int" />
                <constructor-arg index="4" value="${private.password}" />
                <constructor-arg index="5" value="${private.base.data.db}" type="int" />
            </bean>
        </property>
    </bean>

    <!-- 内网-效率统计 -->
    <bean id="statisticsJedisClient" class="com.kuyou.train.common.jedis.JedisClient">
        <property name="jedisPool">
            <bean class="redis.clients.jedis.JedisPool">
                <constructor-arg index="0" ref="jedisPoolConfig" />
                <constructor-arg index="1" value="${private.host}" />
                <constructor-arg index="2" value="${private.port}" type="int" />
                <constructor-arg index="3" value="${private.timeout}" type="int" />
                <constructor-arg index="4" value="${private.password}" />
                <constructor-arg index="5" value="${private.statistics.db}" type="int" />
            </bean>
        </property>
    </bean>

</beans>