<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.train.system.booking.dao.LogMapper">
    <!--可以重用的SQL块，也可以被其他语句引用-->
    <!--
    <sql id="columns">
      id,name
    </sql>
    -->
    
    <insert id="insertBookingLog">
        <selectKey resultType="java.lang.Long" order="AFTER" >
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO cp_orderinfo_history (order_id, order_optlog, create_time, opter) values(#{orderId}, #{message}, now(), #{operator})
    </insert>


    <select id="queryList" resultType="com.train.system.booking.entity.LogReturn">
        SELECT
            return_id,
            return_name,
            return_type,
            return_fail_reason
        FROM
            train_return_optlog
        WHERE
            return_status = '1'
        AND return_ticket = '11'
        AND return_active = '00'
    </select>

</mapper>